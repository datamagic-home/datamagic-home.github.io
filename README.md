# datamagic-home.github.io
Datamagic is a framework that automates data insight video creation via a "Generate-then-Orchestrate" strategy, which first produces candidate scenes and then orchestrates them into a coherent narrative sequence.
